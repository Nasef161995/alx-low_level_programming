#include "main.h"
/**
 * is_prime_number - function that returns 1 if the input integer prime number  
 *
 * @n: parameter is integer
 *
 * Return: returns (1 or 0 ).
 */

int is_prime_number(int n)
{
if (n <= 0)
return (0);
if()

return (1);
}
