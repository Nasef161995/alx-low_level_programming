#include "main.h"
/**
* _abs - prints the absolute value of a number
*@d: number targeted
*Return: returns 0
*/
int _abs(int d)
{
if (d > 0)
return (d);
else if (d < 0)
return (-d);
else
return (0);
}
