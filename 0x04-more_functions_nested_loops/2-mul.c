#include "main.h"
/**
 * mul - is function is for multiplie two integers
 *
 * @a: parameter is an integer
 *
 * @b: parameter is an integer
 *
 * Return: returns 0
 */

int mul(int a, int b)
{
return (a * b);
}
