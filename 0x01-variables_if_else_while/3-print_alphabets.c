#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char (C) = 'a';
	while (c <= 'z'){
	putchar(c);
	c++;
	}
	char (C) = 'A';
	while (c <= 'Z'){
	putchar(c);
	c++;
	}
	return (0);
}

