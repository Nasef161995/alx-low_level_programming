#include "main.h"
/**
*print_alphabet - function that print alphabet
*it prints in lowercase
*Return: no return
*/
void print_alphabet(void)
{
int d;
for (d = 'a'; d <= 'z'; d++)
{
_putchar (d);
}
_putchar (10);
}
